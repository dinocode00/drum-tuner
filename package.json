{
  "name": "drum-tuner",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Drum tuner PWA for iPhone: lug tuning, fundamental pitch, reso-head ratios, target sounds.",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "e2e": "node scripts/e2e.mjs",
    "serve": "npx http-server -c-1 -p 8080 ."
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
